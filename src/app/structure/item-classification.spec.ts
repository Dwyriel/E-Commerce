import { ItemClassification } from './item-classification';

describe('ItemClassification', () => {
  it('should create an instance', () => {
    expect(new ItemClassification()).toBeTruthy();
  });
});
